# Runtime dependencies actually imported by ECHO backend.
# (Previous Emergent scaffold freeze listed 120+ packages; only these are used.)

# API
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.24
pydantic==2.12.5
python-dotenv==1.2.2

# Storage
motor==3.3.1
pymongo==4.5.0

# Document intake
pypdf==6.10.2
python-docx==1.2.0

# Speech providers (selected via SPEECH_PROVIDER)
piper-tts==1.4.2
elevenlabs==2.45.0
openai==1.99.9
httpx==0.28.1

# Local sample-voice clone provider (backend/clone_tts.py) — used regardless of
# SPEECH_PROVIDER for the echo/patricia/martin-en/martin-fr voice ids.
# torch is CPU-only on this host; installing plain `torch==2.13.0` from PyPI pulls
# the default (much larger, CUDA-bundled) build. Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
transformers==5.14.1
speechbrain==1.1.0

# French clone-voice path (patricia/martin-fr): MeloTTS (French synthesis) + OpenVoice V2
# (tone-color conversion into the sample's voice). librosa is pinned down from 0.11.0 to
# match MeloTTS's requirement (verified: no regression to the SpeechT5/English path).
#
# melotts/openvoice themselves are installed SEPARATELY, after this file, with --no-deps —
# their own upstream requirements.txt pins versions (mecab-python3==1.0.9, fugashi==1.3.0,
# transformers==4.27.4, ...) that predate Python 3.13 and have no prebuilt wheel, forcing a
# source build that needs system MeCab/a newer Rust toolchain than may be available. The
# versions below are verified-working substitutes with real cp313 wheels. Setup:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#   pip install --no-deps git+https://github.com/myshell-ai/MeloTTS.git git+https://github.com/myshell-ai/OpenVoice.git
# The unidic dictionary (~526MB) and OpenVoice V2 checkpoints (~131MB) auto-download on
# first use (backend/.cache/openvoice/); MyShell's own S3 checkpoint bucket referenced in
# OpenVoice's docs is dead (confirmed 2026-08-02) — clone_tts.py pulls from the HF mirror
# (huggingface.co/myshell-ai/OpenVoiceV2) instead.
librosa==0.9.1
txtsplit
cached_path
num2words==0.5.12
unidic_lite==1.0.8
unidic==1.1.0
mecab-python3
pykakasi==2.2.1
fugashi
g2p_en==2.1.0
anyascii==0.3.2
jamo==0.4.1
gruut[fr]==2.2.3
g2pkk
pydub==0.25.1
eng_to_ipa==0.0.2
unidecode==1.3.7
pypinyin==0.50.0
cn2an==0.5.22
jieba==0.42.1
langid==1.1.6
loguru==0.7.2
soxr
faster_whisper
wavmark
whisper_timestamped
# librosa 0.9.1 imports pkg_resources, removed from setuptools>=81.
setuptools<81

# Tests
pytest==9.0.3
anyio==4.13.0
